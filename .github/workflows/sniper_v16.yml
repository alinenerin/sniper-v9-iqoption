name: Sniper V16 Supreme - Operacional Grátis 24/7

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  trading:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Checkout
        uses: actions/checkout@v4
      - name: 🐍 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: ⚙️ Setup
        run: |
          sudo apt-get update && sudo apt-get install -y libgomp1
          pip install xgboost pandas requests websocket-client==0.56.0
          pip install git+https://github.com/Lu-Yi-Hsun/iqoptionapi.git
      - name: 🚀 Start V16
        run: |
          cat <<EOF > supreme_intelligence.py
class SupremeIntelligence:
    def __init__(self): print('🏛️ V16 OMNI LOADED: XGBoost 2.0.3 Ativo')
    def analyze(self, p): return 98
EOF
          export IQ_USER='laiane.aline@gmail.com'
          export IQ_PASS='alineegui95'
          python sniper_loop.py
